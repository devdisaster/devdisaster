import type { GenericId } from "convex/values";

export async function check(
  _clusterId: GenericId<"clusters">,
): Promise<never> {
  throw new Error("todo");
}

export async function forceThreshold(
  _clusterId: GenericId<"clusters">,
): Promise<never> {
  throw new Error("todo");
}
